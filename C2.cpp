#include <stdio.h>
// y line hai comment yani ki nhi aiga code mai or (*\ /*) multiple comment kliye
// output 
int main() { 
 
  int age= 22;
  float pi = 3.14;
  char star = ('*');// use only single upper wala comma for character
  
  printf("age is %d \n",age); // (%d) jo hai integer ko denote krega or \n) jo hai line break k liye 
  printf("value of pi is %f \n",pi); // (real numbers k liye %f use krtai)
  printf("star = %c \n",star); // (%c) jo kisi charater store kea hua ko denote krnai k liye
  
  
  
  
  return 0;
}